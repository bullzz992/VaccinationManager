@model IEnumerable<VaccinationManager.Models.VaccinationPrice>
@{
    ViewBag.CurrentPage = "Vaccination Prices";

}

<h2 style="background-color: #e57c58; color: white; display: table; width: calc(100%); height: 50px; border-bottom: 3px solid #eee; padding-left: 20px;">Vaccination Prices</h2>
<div style="background-color: #e57c58; width: calc(100%); height: 5px;"></div>
    @*<p>
        @Html.ActionLink("Create New", "Create", null, htmlAttributes: new { @class = "btnCreateNew" })
    </p>*@
    <table class="table">
        <tr>
            <th>
                @Html.DisplayName("Vaccination Definition")
            </th>
            <th>
                @Html.DisplayName("Amount")
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @*@Html.HiddenFor(modelItem => item.VaccinationDefId)*@
                @ViewBag.DefDescription[item.VaccinationDefId].ToString()
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PriceAmount)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.VaccinationPriceId }) 
            </td>
        </tr>
    }
    
    </table>

