@model FileContentResult


<h2 style="background-color: #e57c58; color: white; display: table; width: calc(100%); height: 50px; border-bottom: 3px solid #eee; padding-left: 20px;">Invoice Ready for Download</h2>
<div style="background-color: #e57c58; width: calc(100%); height: 5px;"></div>
<br />
<div class="container-fluid">

    @using (Html.BeginForm("Edit", "Invoice", FormMethod.Post, new {id = Url.RequestContext.RouteData.Values["id"]}))
    {
        @Html.Hidden("id", Url.RequestContext.RouteData.Values["id"])
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" id="submitBtn" value="Click to View Invoice" formtarget="_blank" class="btn btn-default"/>
            </div>
        </div>

    }
</div>